---
phase: 07-campos-cex-nota-medica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/expedientes/schemas/nota-medica.schema.ts
  - backend/src/modules/expedientes/dto/create-nota-medica.dto.ts
autonomous: true

must_haves:
  truths:
    - "Backend acepta 11 nuevos campos (8 CEX + indiceMasaCorporal, categoriaIMC, categoriaCircunferenciaCintura) en POST/PATCH nota médica"
    - "Campos opcionales: notas existentes sin estos campos siguen funcionando"
    - "Validación condicional activa: peso fuera de 1-400 (y ≠999) es rechazado, glucemia=0 no requiere tipoMedicion"
    - "derechohabiencia acepta formato string con '&' separador (ej: '2&3&5')"
  artifacts:
    - path: "backend/src/modules/expedientes/schemas/nota-medica.schema.ts"
      provides: "11 nuevos campos @Prop({ required: false })"
      contains: "genero?: number"
    - path: "backend/src/modules/expedientes/dto/create-nota-medica.dto.ts"
      provides: "11 validaciones con @ValidateIf, @Min, @Max, @IsIn, @Matches"
      contains: "derechohabiencia?: string"
  key_links:
    - from: "create-nota-medica.dto.ts"
      to: "nota-medica.schema.ts"
      via: "Nombres de campo idénticos"
      pattern: "genero|derechohabiencia|peso|talla|circunferenciaCintura|glucemia|tipoMedicion|resultadoObtenidoaTravesde|indiceMasaCorporal|categoriaIMC|categoriaCircunferenciaCintura"
---

<objective>
Agregar 11 nuevos campos al schema Mongoose y DTO de nota médica: los 8 campos CEX (genero, derechohabiencia, peso, talla, circunferenciaCintura, glucemia, tipoMedicion, resultadoObtenidoaTravesde) más 3 campos calculados (indiceMasaCorporal, categoriaIMC, categoriaCircunferenciaCintura).

Purpose: Sin estos campos en el backend, el frontend no puede guardar los datos y el mapper CEX seguirá usando valores hardcoded.
Output: Schema y DTO actualizados, update DTO hereda automáticamente via PartialType.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/.planning/ROADMAP.md
@backend/.planning/STATE.md
@backend/.planning/phases/07-campos-cex-nota-medica/CONTEXT.md
@backend/.planning/phases/07-campos-cex-nota-medica/RESEARCH.md
@backend/src/modules/expedientes/schemas/nota-medica.schema.ts
@backend/src/modules/expedientes/dto/create-nota-medica.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar 11 campos al schema Mongoose</name>
  <files>backend/src/modules/expedientes/schemas/nota-medica.schema.ts</files>
  <action>
Agregar 11 campos después de `saturacionOxigeno` (línea 42) en nota-medica.schema.ts:

```typescript
// CEX: Datos demográficos
@Prop({ required: false })
genero?: number; // 0=No especificado, 1=Masculino, 2=Femenino, 3=Transgénero, 4=Transexual, 5=Travesti, 6=Intersexual, 88=Otro

@Prop({ required: false })
derechohabiencia?: string; // "0", "2&3", "99" etc.

// CEX: Somatometría
@Prop({ required: false })
peso?: number; // kg, 1-400, 999=desconoce

@Prop({ required: false })
talla?: number; // cm, 30-220, 999=desconoce

@Prop({ required: false })
circunferenciaCintura?: number; // cm, 20-300, 0=desconoce

@Prop({ required: false })
indiceMasaCorporal?: number; // calculado: peso / (talla/100)^2

@Prop({ required: false })
categoriaIMC?: string; // "Bajo peso", "Normal", "Sobrepeso", "Obesidad I/II/III"

@Prop({ required: false })
categoriaCircunferenciaCintura?: string; // "Normal", "Riesgo elevado", "Riesgo muy elevado"

// CEX: Glucemia
@Prop({ required: false })
glucemia?: number; // mg/dl, 20-999, 0=desconoce

@Prop({ required: false })
tipoMedicion?: number; // 0=No ayunas, 1=Ayunas, -1=NA

@Prop({ required: false })
resultadoObtenidoaTravesde?: number; // 1=Lab, 2=Tira, -1=NA
```

Todos con `required: false` para backward compatibility con documentos existentes.
  </action>
  <verify>
`cd backend && npx tsc --noEmit` compila sin errores.
  </verify>
  <done>nota-medica.schema.ts tiene 11 nuevos campos @Prop({ required: false }), el archivo compila.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar 11 validaciones al DTO</name>
  <files>backend/src/modules/expedientes/dto/create-nota-medica.dto.ts</files>
  <action>
Agregar import de `Matches` a la lista de imports de class-validator (línea 1-17).

Agregar 11 campos validados después del bloque de `saturacionOxigeno` (línea 97), antes del campo `diagnostico`:

```typescript
// CEX: Datos demográficos
@IsOptional()
@IsNumber({ maxDecimalPlaces: 0 })
@IsIn([0, 1, 2, 3, 4, 5, 6, 88], { message: 'genero: valor no válido (0-6, 88)' })
genero?: number;

@IsOptional()
@IsString()
@Matches(/^(\d{1,2})(&\d{1,2}){0,8}$/, { message: 'derechohabiencia: formato inválido (ej: "2&3&5")' })
derechohabiencia?: string;

// CEX: Somatometría
@IsOptional()
@IsNumber({ maxDecimalPlaces: 3 })
@ValidateIf((o) => o.peso != null && o.peso !== 999)
@Min(1, { message: 'CEX: peso mínimo 1 kg' })
@Max(400, { message: 'CEX: peso máximo 400 kg' })
peso?: number;

@IsOptional()
@IsNumber({ maxDecimalPlaces: 0 })
@ValidateIf((o) => o.talla != null && o.talla !== 999)
@Min(30, { message: 'CEX: talla mínima 30 cm' })
@Max(220, { message: 'CEX: talla máxima 220 cm' })
talla?: number;

@IsOptional()
@IsNumber({ maxDecimalPlaces: 0 })
@ValidateIf((o) => o.circunferenciaCintura != null && o.circunferenciaCintura !== 0)
@Min(20, { message: 'CEX: circunferencia cintura mínima 20 cm' })
@Max(300, { message: 'CEX: circunferencia cintura máxima 300 cm' })
circunferenciaCintura?: number;

@IsOptional()
@IsNumber({ maxDecimalPlaces: 2 })
indiceMasaCorporal?: number;

@IsOptional()
@IsString()
categoriaIMC?: string;

@IsOptional()
@IsString()
categoriaCircunferenciaCintura?: string;

// CEX: Glucemia
@IsOptional()
@IsNumber({ maxDecimalPlaces: 0 })
@ValidateIf((o) => o.glucemia != null && o.glucemia !== 0)
@Min(20, { message: 'CEX: glucemia mínima 20 mg/dl' })
@Max(999, { message: 'CEX: glucemia máxima 999 mg/dl' })
glucemia?: number;

@IsOptional()
@IsNumber({ maxDecimalPlaces: 0 })
@ValidateIf((o) => o.glucemia != null && o.glucemia !== 0)
@IsIn([0, 1], { message: 'tipoMedicion: 0=No ayunas, 1=Ayunas' })
tipoMedicion?: number;

@IsOptional()
@IsNumber({ maxDecimalPlaces: 0 })
@ValidateIf((o) => o.glucemia != null && o.glucemia !== 0)
@IsIn([1, 2], { message: 'resultadoObtenidoaTravesde: 1=Laboratorio, 2=Tira glucosa' })
resultadoObtenidoaTravesde?: number;
```

Notas clave:
- `Matches` importar de class-validator (no existe actualmente en los imports)
- `@ValidateIf` para tipoMedicion y resultadoObtenidoaTravesde referencia `o.glucemia` (solo validar si glucemia≠0)
- `@ValidateIf` para peso/talla referencia el valor "desconoce" (999), para circunferenciaCintura referencia 0
- update-nota-medica.dto.ts hereda via PartialType — NO requiere cambios manuales
  </action>
  <verify>
`cd backend && npx tsc --noEmit` compila sin errores.
Verificar que Matches está en los imports:
`grep "Matches" backend/src/modules/expedientes/dto/create-nota-medica.dto.ts`
  </verify>
  <done>create-nota-medica.dto.ts tiene 11 campos validados con decoradores correctos, Matches importado, compila sin errores.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — sin errores de compilación
2. Verificar que los 11 campos aparecen en ambos archivos:
   `grep -c "genero\|derechohabiencia\|peso\|talla\|circunferenciaCintura\|glucemia\|tipoMedicion\|resultadoObtenidoaTravesde\|indiceMasaCorporal\|categoriaIMC\|categoriaCircunferenciaCintura" backend/src/modules/expedientes/schemas/nota-medica.schema.ts`
   (debe ser ≥11)
3. Verificar que update DTO no necesita cambios (hereda via PartialType):
   `grep "PartialType" backend/src/modules/expedientes/dto/update-nota-medica.dto.ts`
</verification>

<success_criteria>
- 11 nuevos campos en nota-medica.schema.ts (todos con @Prop({ required: false }))
- 11 validaciones en create-nota-medica.dto.ts con decoradores correctos
- Matches importado de class-validator
- Backend compila sin errores
- Documentos existentes sin los 11 campos siguen siendo válidos (backward compatible)
</success_criteria>

<output>
After completion, create `backend/.planning/phases/07-campos-cex-nota-medica/07-01-SUMMARY.md`
</output>
