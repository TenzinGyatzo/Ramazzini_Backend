---
phase: 07-campos-cex-nota-medica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/steps/notaMedicaSteps/Step3.vue (NUEVO → Género/Derechohabiencia)
  - frontend/src/components/steps/notaMedicaSteps/Step7.vue (NUEVO → Somatometría)
  - frontend/src/components/steps/notaMedicaSteps/Step8.vue (NUEVO → Glucemia)
  - frontend/src/components/steps/notaMedicaSteps/Step4.vue (RENOMBRADO desde Step3)
  - frontend/src/components/steps/notaMedicaSteps/Step5.vue (RENOMBRADO desde Step4)
  - frontend/src/components/steps/notaMedicaSteps/Step6.vue (RENOMBRADO desde Step5)
  - frontend/src/components/steps/notaMedicaSteps/Step9.vue (RENOMBRADO desde Step6)
  - frontend/src/components/steps/notaMedicaSteps/Step10.vue (RENOMBRADO desde Step7)
  - frontend/src/components/steps/notaMedicaSteps/Step11.vue (RENOMBRADO desde Step8)
  - frontend/src/components/steps/notaMedicaSteps/Step12.vue (RENOMBRADO desde Step9)
  - frontend/src/components/steps/notaMedicaSteps/Step13.vue (RENOMBRADO desde Step10)
  - frontend/src/components/steps/notaMedicaSteps/Step14.vue (RENOMBRADO desde Step11)
  - frontend/src/components/steps/notaMedicaSteps/Step9.spec.ts (RENOMBRADO desde Step6.spec.ts)
  - frontend/src/components/steps/FormStepper.vue
  - frontend/src/helpers/validacionCampos.ts
autonomous: true

must_haves:
  truths:
    - "Formulario nota médica muestra 14 steps para SIRES_NOM024 y 11 steps para SIN_REGIMEN"
    - "Los 3 nuevos steps (Step3, Step7, Step8) SOLO aparecen para providers SIRES_NOM024"
    - "Step3 captura género (select, pre-selección desde trabajador.sexo) y derechohabiencia (checkboxes múltiples con exclusividad)"
    - "Step7 captura peso (kg), talla (cm), circunferencia cintura (cm) con checkboxes 'Se desconoce', calcula IMC y categorías"
    - "Step8 captura glucemia (mg/dl) con checkbox 'Se desconoce', y condicionalmente tipoMedicion y resultadoObtenidoaTravesde"
    - "Steps renumerados conservan su funcionalidad (ej: Step9 = diagnóstico principal, antes Step6)"
    - "Providers SIN_REGIMEN mantienen comportamiento idéntico al actual (11 steps, sin cambios visibles)"
    - "VisualizadorNotaMedica: secciones nuevas solo visibles para SIRES; step numbers dinámicos según régimen"
    - "Validación pre-submit lleva al paso correcto según régimen (sistólica/diastólica → paso dinámico)"
  artifacts:
    - path: "frontend/src/components/steps/notaMedicaSteps/Step3.vue"
      provides: "Captura género y derechohabiencia"
    - path: "frontend/src/components/steps/notaMedicaSteps/Step7.vue"
      provides: "Captura somatometría (peso, talla, cintura, IMC)"
    - path: "frontend/src/components/steps/notaMedicaSteps/Step8.vue"
      provides: "Captura glucemia y campos dependientes"
    - path: "frontend/src/components/steps/FormStepper.vue"
      provides: "14 imports y array de 14 steps para notaMedica"
  key_links:
    - from: "Step3.vue / Step7.vue / Step8.vue"
      to: "formDataNotaMedica (formDataStore)"
      via: "watch + onUnmounted syncFormData"
      pattern: "formDataNotaMedica\\.(genero|peso|talla|glucemia)"
    - from: "FormStepper.vue"
      to: "notaMedicaSteps/Step*.vue"
      via: "import + stepsStore.setSteps array"
      pattern: "Step\\d+NotaMedica"
    - from: "validacionCampos.ts"
      to: "FormStepper goToStep"
      via: "paso: N en return value"
      pattern: "paso: 6"
---

<objective>
Crear 3 nuevos steps Vue para el formulario de nota médica (Step3 Género/Derechohabiencia, Step7 Somatometría, Step8 Glucemia), renumerar los 9 steps existentes (3→4, 4→5, 5→6, 6→9, 7→10, 8→11, 9→12, 10→13, 11→14), actualizar FormStepper.vue (imports + array de 14 steps) y corregir validarNotaMedicaPreSubmit.

Purpose: Sin los steps frontend no hay forma de capturar los 8 campos CEX en el formulario de nota médica.
Output: 14 steps funcionales, 3 nuevos componentes Vue, FormStepper actualizado, validación corregida.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/.planning/ROADMAP.md
@backend/.planning/STATE.md
@backend/.planning/phases/07-campos-cex-nota-medica/CONTEXT.md
@backend/.planning/phases/07-campos-cex-nota-medica/RESEARCH.md
@frontend/src/components/steps/notaMedicaSteps/Step5.vue (patrón "Se desconoce" a replicar)
@frontend/src/components/steps/exploracionFisicaSteps/Step2.vue (patrón somatometría a replicar)
@frontend/src/components/steps/FormStepper.vue (imports líneas 130-140, array líneas 626-639)
@frontend/src/helpers/validacionCampos.ts (validarNotaMedicaPreSubmit líneas 668-723)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renumerar steps existentes y crear 3 nuevos steps</name>
  <files>
    frontend/src/components/steps/notaMedicaSteps/Step3.vue (NUEVO)
    frontend/src/components/steps/notaMedicaSteps/Step7.vue (NUEVO)
    frontend/src/components/steps/notaMedicaSteps/Step8.vue (NUEVO)
    frontend/src/components/steps/notaMedicaSteps/Step4.vue (antes Step3)
    frontend/src/components/steps/notaMedicaSteps/Step5.vue (antes Step4)
    frontend/src/components/steps/notaMedicaSteps/Step6.vue (antes Step5)
    frontend/src/components/steps/notaMedicaSteps/Step9.vue (antes Step6)
    frontend/src/components/steps/notaMedicaSteps/Step10.vue (antes Step7)
    frontend/src/components/steps/notaMedicaSteps/Step11.vue (antes Step8)
    frontend/src/components/steps/notaMedicaSteps/Step12.vue (antes Step9)
    frontend/src/components/steps/notaMedicaSteps/Step13.vue (antes Step10)
    frontend/src/components/steps/notaMedicaSteps/Step14.vue (antes Step11)
    frontend/src/components/steps/notaMedicaSteps/Step9.spec.ts (antes Step6.spec.ts)
  </files>
  <action>
**Orden de renumeración (IMPORTANTE — de mayor a menor para evitar conflictos):**

1. Renombrar Step11.vue → Step14.vue
2. Renombrar Step10.vue → Step13.vue
3. Renombrar Step9.vue → Step12.vue
4. Renombrar Step8.vue → Step11.vue
5. Renombrar Step7.vue → Step10.vue
6. Renombrar Step6.vue → Step9.vue
7. Renombrar Step6.spec.ts → Step9.spec.ts
8. Renombrar Step5.vue → Step6.vue
9. Renombrar Step4.vue → Step5.vue
10. Renombrar Step3.vue → Step4.vue

Usar `git mv` para cada uno (preserva historial). Ejemplo:
```bash
cd frontend/src/components/steps/notaMedicaSteps
git mv Step11.vue Step14.vue
git mv Step10.vue Step13.vue
# ... etc, de mayor a menor
```

Después, crear los 3 nuevos steps:

**Step3.vue (Género y Derechohabiencia):**
- Usar `<script setup>` con Composition API
- Imports: `ref, watch, onMounted, onUnmounted` de vue, `useFormDataStore`, `useDocumentosStore`, `useTrabajadoresStore`
- `genero` ref (number, default 0)
- `derechohabienciaSeleccion` ref (array de strings seleccionados)
- Pre-seleccionar género desde `trabajadores.currentTrabajador.sexo`: "Masculino"→1, "Femenino"→2
- Opciones de género: `[{value: 0, label: 'No especificado'}, {value: 1, label: 'Masculino'}, {value: 2, label: 'Femenino'}, {value: 3, label: 'Transgénero'}, {value: 4, label: 'Transexual'}, {value: 5, label: 'Travesti'}, {value: 6, label: 'Intersexual'}, {value: 88, label: 'Otro'}]`
- Opciones de derechohabiencia: `[{value: '0', label: 'No especificado'}, {value: '1', label: 'Ninguna'}, {value: '2', label: 'IMSS'}, {value: '3', label: 'ISSSTE'}, {value: '4', label: 'PEMEX'}, {value: '5', label: 'SEDENA'}, {value: '6', label: 'SEMAR'}, {value: '8', label: 'Otra'}, {value: '10', label: 'IMSS Bienestar'}, {value: '11', label: 'ISSFAM'}, {value: '14', label: 'OPD IMSS BIENESTAR'}, {value: '99', label: 'Se ignora'}]`
- Lógica de exclusividad derechohabiencia: si selecciona '0', '1' o '99' → desmarcar todas las demás y no permitir combinar. Si selecciona otro → desmarcar '0', '1', '99' si estaban. Máximo 9 selecciones.
- `syncFormData()`: `formDataNotaMedica.genero = genero.value`, `formDataNotaMedica.derechohabiencia = derechohabienciaSeleccion.value.join('&')` (o '0' si vacío)
- `getValFromSource(field, defaultVal)` igual que Step5/Step6: prioridad formData > currentDocument > default
- `onMounted`: cargar de formData o currentDocument. Para derechohabiencia: parsear string '2&3' → array ['2','3']
- `watch` profundo sobre genero y derechohabienciaSeleccion → syncFormData()
- `onUnmounted`: syncFormData() final
- Template: `<select>` para género, grupo de checkboxes para derechohabiencia con labels

**Step7.vue (Somatometría):**
- Replicar estructura de `exploracionFisicaSteps/Step2.vue` adaptando:
  - Store: `formDataNotaMedica` en lugar de `formDataExploracionFisica`
  - Campo: `talla` en cm (input number, rango 30-220) NO `altura` en metros
  - Campo: `peso` en kg (input number, rango 1-400, max 3 decimales)
  - Campo: `circunferenciaCintura` en cm (input number, rango 20-300)
  - Agregar checkbox `seDesconocePeso` → pone peso=999
  - Agregar checkbox `seDesconoceTalla` → pone talla=999
  - Agregar checkbox `seDesconoceCircunferencia` → pone circunferenciaCintura=0
  - Cuando "Se desconoce" está activo → deshabilitar input, limpiar valor visual
  - Cálculo IMC: `peso / ((talla / 100) ** 2)` — SOLO si peso≠999 y talla≠999 y ambos>0
  - Función `calcularIMC()` replicada de Step2.vue con conversión talla cm→m
  - Función `setCategoriaIMC(imc)`: <18.5 "Bajo peso", 18.5-24.9 "Normal", 25-29.9 "Sobrepeso", 30-34.9 "Obesidad I", 35-39.9 "Obesidad II", ≥40 "Obesidad III"
  - Función `setCategoriaCircunferenciaCintura(cintura, genero)`: usar genero del formDataNotaMedica (1=Masculino, 2=Femenino) para umbrales. Masculino: ≤90 Normal, 90.1-100 Riesgo elevado, >100 Riesgo muy elevado. Femenino: ≤80 Normal, 80.1-88 Riesgo elevado, >88 Riesgo muy elevado.
  - `syncFormData()`: peso, talla, circunferenciaCintura, indiceMasaCorporal, categoriaIMC, categoriaCircunferenciaCintura
  - Mensajes de error computed para rangos fuera de CEX
  - Patrón getValFromSource / onMounted / watch / onUnmounted

**Step8.vue (Glucemia):**
- Patrón de Step5.vue (signos vitales) adaptado
- Campo `glucemia` (number, rango 20-999, 0=desconoce)
- Checkbox `seDesconoceGlucemia` → pone glucemia=0
- Campos condicionales (solo visibles si glucemia≠0):
  - `tipoMedicion`: radio buttons 0="No (no fue en ayunas)", 1="Sí (fue en ayunas)"
  - `resultadoObtenidoaTravesde`: radio buttons 1="Laboratorio", 2="Tira de glucosa capilar"
- Cuando glucemia=0 (o "Se desconoce"): tipoMedicion=-1, resultadoObtenidoaTravesde=-1 (automático)
- `syncFormData()`: glucemia, tipoMedicion, resultadoObtenidoaTravesde
- Mensajes de error computed para rango glucemia
- Patrón getValFromSource / onMounted / watch / onUnmounted
  </action>
  <verify>
Verificar que los 14 archivos Vue existen:
```bash
ls frontend/src/components/steps/notaMedicaSteps/Step{1..14}.vue | wc -l
# Debe ser 14
```
Verificar que Step9.spec.ts existe:
```bash
ls frontend/src/components/steps/notaMedicaSteps/Step9.spec.ts
```
Verificar que los archivos originales NO existen con sus nombres viejos (Step6.spec.ts no debe existir).
  </verify>
  <done>
14 archivos Step*.vue existen en notaMedicaSteps/. 3 nuevos steps funcionales (Step3 género/derechohabiencia, Step7 somatometría, Step8 glucemia). 9 steps renumerados correctamente. Step9.spec.ts renombrado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar FormStepper.vue y validarNotaMedicaPreSubmit</name>
  <files>
    frontend/src/components/steps/FormStepper.vue
    frontend/src/helpers/validacionCampos.ts
  </files>
  <action>
**FormStepper.vue — Imports (líneas 130-140):**

Reemplazar los 11 imports actuales por 14:
```typescript
import Step1NotaMedica from '../steps/notaMedicaSteps/Step1.vue';
import Step2NotaMedica from '../steps/notaMedicaSteps/Step2.vue';
import Step3NotaMedica from '../steps/notaMedicaSteps/Step3.vue';
import Step4NotaMedica from '../steps/notaMedicaSteps/Step4.vue';
import Step5NotaMedica from '../steps/notaMedicaSteps/Step5.vue';
import Step6NotaMedica from '../steps/notaMedicaSteps/Step6.vue';
import Step7NotaMedica from '../steps/notaMedicaSteps/Step7.vue';
import Step8NotaMedica from '../steps/notaMedicaSteps/Step8.vue';
import Step9NotaMedica from '../steps/notaMedicaSteps/Step9.vue';
import Step10NotaMedica from '../steps/notaMedicaSteps/Step10.vue';
import Step11NotaMedica from '../steps/notaMedicaSteps/Step11.vue';
import Step12NotaMedica from '../steps/notaMedicaSteps/Step12.vue';
import Step13NotaMedica from '../steps/notaMedicaSteps/Step13.vue';
import Step14NotaMedica from '../steps/notaMedicaSteps/Step14.vue';
```

**FormStepper.vue — Array de steps CONDICIONAL (líneas 626-639):**

⚠️ CRITICAL: Los 3 nuevos steps SOLO se incluyen para SIRES_NOM024. Seguir el patrón de `examenVista` (líneas 508-531) que construye el array condicionalmente.

Reemplazar el bloque `notaMedica` por:
```typescript
} else if (documentos.currentTypeOfDocument === 'notaMedica') {
  const notaMedicaSteps = [
    { component: Step1NotaMedica, name: 'Paso 1' },
    { component: Step2NotaMedica, name: 'Paso 2' },
  ];

  // SIRES_NOM024: agregar Step3 (Género/Derechohabiencia)
  if (showSiresUI.value) {
    notaMedicaSteps.push({ component: Step3NotaMedica, name: `Paso ${notaMedicaSteps.length + 1}` });
  }

  notaMedicaSteps.push(
    { component: Step4NotaMedica, name: `Paso ${notaMedicaSteps.length + 1}` },
    { component: Step5NotaMedica, name: `Paso ${notaMedicaSteps.length + 2}` },
    { component: Step6NotaMedica, name: `Paso ${notaMedicaSteps.length + 3}` },
  );

  // SIRES_NOM024: agregar Step7 (Somatometría) y Step8 (Glucemia)
  if (showSiresUI.value) {
    notaMedicaSteps.push(
      { component: Step7NotaMedica, name: `Paso ${notaMedicaSteps.length + 1}` },
      { component: Step8NotaMedica, name: `Paso ${notaMedicaSteps.length + 1}` },
    );
  }

  notaMedicaSteps.push(
    { component: Step9NotaMedica, name: `Paso ${notaMedicaSteps.length + 1}` },
    { component: Step10NotaMedica, name: `Paso ${notaMedicaSteps.length + 2}` },
    { component: Step11NotaMedica, name: `Paso ${notaMedicaSteps.length + 3}` },
    { component: Step12NotaMedica, name: `Paso ${notaMedicaSteps.length + 4}` },
    { component: Step13NotaMedica, name: `Paso ${notaMedicaSteps.length + 5}` },
    { component: Step14NotaMedica, name: `Paso ${notaMedicaSteps.length + 6}` },
  );

  stepsStore.setSteps(notaMedicaSteps);
```

Resultado:
- SIRES_NOM024: 14 steps (con género/derechohabiencia, somatometría, glucemia)
- SIN_REGIMEN: 11 steps (idéntico al comportamiento actual)

`showSiresUI` ya está disponible como computed en FormStepper.vue (línea 327).

**FormStepper.vue — components (línea 310):**
Verificar si los componentes Step12/13/14NotaMedica están registrados en la sección `components` del componente (si usa Options API parcial). Si es `<script setup>`, los imports bastan. Adaptar según el patrón encontrado.

**VisualizadorNotaMedica.vue — Condicionalidad SIRES + step numbers dinámicos:**

⚠️ CRITICAL: El VisualizadorNotaMedica.vue usa `steps.currentStep === N` y `goToStep(N)` hardcoded. Con la inclusión condicional de steps, las posiciones cambian:

| Sección | Step en SIRES | Step en SIN_REGIMEN |
|---------|---------------|---------------------|
| Tipo/Fecha | 1 | 1 |
| Motivo | 2 | 2 |
| Género/Derechohabiencia | 3 | (no existe) |
| Antecedentes | 4 | 3 |
| Exploración Física | 5 | 4 |
| Signos Vitales | 6 | 5 |
| Somatometría | 7 | (no existe) |
| Glucemia | 8 | (no existe) |
| Diagnóstico Principal | 9 | 6 |
| Comorbilidad 2 | 10 | 7 |
| Comorbilidad 3 | 11 | 8 |
| Tratamiento | 12 | 9 |
| Recomendaciones | 13 | 10 |
| Observaciones | 14 | 11 |

Implementar con un computed `stepMap` que mapee secciones a números de paso:
```typescript
import { useNom024Fields } from '@/composables/useNom024Fields';
const { isSIRES } = useNom024Fields();

const stepMap = computed(() => {
  if (isSIRES.value) {
    return { genero: 3, antecedentes: 4, exploracion: 5, signos: 6, somatometria: 7, glucemia: 8, diagnostico: 9, comorbilidad2: 10, comorbilidad3: 11, tratamiento: 12, recomendaciones: 13, observaciones: 14 };
  }
  return { antecedentes: 3, exploracion: 4, signos: 5, diagnostico: 6, comorbilidad2: 7, comorbilidad3: 8, tratamiento: 9, recomendaciones: 10, observaciones: 11 };
});
```

Luego reemplazar todos los hardcoded:
- `goToStep(3)` → `goToStep(stepMap.antecedentes)` (antecedentes)
- `steps.currentStep === 5` → `steps.currentStep === stepMap.signos` (signos vitales)
- etc.

Agregar 3 secciones nuevas con `v-if="isSIRES"`:
- Sección Género/Derechohabiencia (después de Motivo, antes de Antecedentes)
- Sección Somatometría (después de Signos Vitales, antes de Diagnóstico)
- Sección Glucemia (después de Somatometría, antes de Diagnóstico)

**validacionCampos.ts — paso dinámico según régimen:**

La función `validarNotaMedicaPreSubmit` usa `paso: 5` hardcoded para signos vitales. Con la condicionalidad SIRES, el paso de signos vitales es 6 (SIRES) o 5 (SIN_REGIMEN).

Opción recomendada: la función debe recibir un parámetro `isSIRES` (o un `stepMap`) y usar el paso correcto:
```typescript
// ANTES:
paso: 5,

// DESPUÉS:
paso: isSIRES ? 6 : 5,
```

Buscar TODOS los `paso:` hardcoded en validaciones de nota médica y aplicar la misma lógica condicional.

**validacionCampos.ts — validarNotaMedicaCIEExact4Chars (verificar):**
Si esta función retorna `paso: 6` (diagnóstico principal), cambiar a `paso: isSIRES ? 9 : 6`.
  </action>
  <verify>
1. Contar imports en FormStepper.vue:
   `grep -c "import Step.*NotaMedica" frontend/src/components/steps/FormStepper.vue`
   Debe ser 14.
2. Contar entries en array de steps:
   `grep -c "Step.*NotaMedica" frontend/src/components/steps/FormStepper.vue`
   Debe ser ≥28 (14 imports + 14 en array).
3. Verificar que paso de signos vitales es 6:
   `grep "paso: 6" frontend/src/helpers/validacionCampos.ts`
4. Verificar que no queda `paso: 5` para signos vitales:
   `grep "paso: 5" frontend/src/helpers/validacionCampos.ts` — no debe haber match de la validación sistólica/diastólica.
  </verify>
  <done>
FormStepper.vue tiene 14 imports y array de 14 steps. validarNotaMedicaPreSubmit usa paso:6 para signos vitales. Todas las referencias paso:N actualizadas a la nueva numeración.
  </done>
</task>

</tasks>

<verification>
1. Los 14 archivos Step*.vue existen en notaMedicaSteps/
2. FormStepper.vue importa y registra 14 steps para notaMedica
3. Navegación entre pasos funciona (1→14)
4. Step3.vue: select de género y checkboxes de derechohabiencia renderizan correctamente
5. Step7.vue: inputs de peso/talla/cintura con checkboxes "Se desconoce" y cálculo IMC
6. Step8.vue: input glucemia con campos condicionales tipoMedicion/resultadoObtenidoaTravesde
7. validarNotaMedicaPreSubmit: paso correcto para cada validación
8. Los datos se sincronizan con formDataNotaMedica en cada step
</verification>

<success_criteria>
- SIRES_NOM024: 14 steps renderizan al crear/editar nota médica
- SIN_REGIMEN: 11 steps renderizan (comportamiento IDÉNTICO al actual, sin cambios visibles)
- Los 3 nuevos steps NO aparecen para providers SIN_REGIMEN
- Género pre-seleccionado desde trabajador.sexo (solo SIRES)
- Derechohabiencia con exclusividad (0/1/99 exclusivos, resto combinables hasta 9)
- IMC y categorías calculados automáticamente
- Glucemia=0 → tipoMedicion y resultadoObtenidoaTravesde ocultos/deshabilitados (=-1)
- Steps renumerados conservan toda su funcionalidad
- VisualizadorNotaMedica: secciones nuevas solo visibles para SIRES, step numbers correctos en ambos regímenes
- Validación pre-submit navega al paso correcto según régimen
</success_criteria>

<output>
After completion, create `backend/.planning/phases/07-campos-cex-nota-medica/07-02-SUMMARY.md`
</output>
