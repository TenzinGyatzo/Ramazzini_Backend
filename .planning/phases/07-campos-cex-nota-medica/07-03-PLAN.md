---
phase: 07-campos-cex-nota-medica
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/src/modules/giis-export/transformers/cex.mapper.ts
  - backend/src/modules/informes/documents/nota-medica.informe.ts
autonomous: true

must_haves:
  truths:
    - "Mapper CEX lee los 8 campos desde el documento consulta en lugar de valores hardcoded"
    - "Mapper CEX mantiene fallbacks correctos: peso→999, talla→999, cintura→0, glucemia→0, tipoMedicion→-1, resultadoObtenidoaTravesde→-1, genero→sexoCURP→0, derechohabiencia→'99'"
    - "PDF de nota médica incluye sección Somatometría (peso, talla, IMC, cintura) cuando hay datos"
    - "PDF de nota médica incluye sección Glucemia (valor, tipo medición, origen) cuando hay datos"
    - "PDF de nota médica incluye Género y Derechohabiencia cuando hay datos"
  artifacts:
    - path: "backend/src/modules/giis-export/transformers/cex.mapper.ts"
      provides: "ConsultaExternaLike con 8 campos CEX; valueByField lee de consulta con fallback"
      contains: "consulta.peso ?? 999"
    - path: "backend/src/modules/informes/documents/nota-medica.informe.ts"
      provides: "Interface NotaMedica con 11 campos; funciones construirSomatometria y construirGlucemia"
      contains: "indiceMasaCorporal"
  key_links:
    - from: "cex.mapper.ts valueByField"
      to: "consulta (NotaMedica document)"
      via: "consulta.peso, consulta.genero, etc."
      pattern: "consulta\\.(peso|talla|genero|glucemia)"
    - from: "nota-medica.informe.ts content array"
      to: "construirSomatometria / construirGlucemia"
      via: "Inserción en content[] después de construirSignosVitales"
      pattern: "construirSomatometria|construirGlucemia"
---

<objective>
Actualizar el mapper GIIS CEX para leer los 8 campos desde la nota médica (en lugar de valores hardcoded) y actualizar la generación PDF para incluir secciones de somatometría, glucemia, y datos demográficos.

Purpose: Sin estos cambios, el export GIIS CEX seguirá usando defaults aunque el usuario capture los datos, y el PDF no mostrará la información capturada.
Output: Mapper CEX alimentado con datos reales, PDF con 3 nuevas secciones opcionales.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/.planning/ROADMAP.md
@backend/.planning/STATE.md
@backend/.planning/phases/07-campos-cex-nota-medica/CONTEXT.md
@backend/.planning/phases/07-campos-cex-nota-medica/RESEARCH.md
@backend/src/modules/giis-export/transformers/cex.mapper.ts
@backend/src/modules/informes/documents/nota-medica.informe.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar mapper CEX — interface y valueByField</name>
  <files>backend/src/modules/giis-export/transformers/cex.mapper.ts</files>
  <action>
**1. Extender interface ConsultaExternaLike (líneas 23-44):**

Agregar los 8 campos opcionales antes del index signature `[key: string]: unknown`:
```typescript
genero?: number;
derechohabiencia?: string;
peso?: number;
talla?: number;
circunferenciaCintura?: number;
glucemia?: number;
tipoMedicion?: number;
resultadoObtenidoaTravesde?: number;
```

**2. Actualizar valueByField — genero (línea 347):**

ANTES: `genero: genero ?? 0,`
DESPUÉS: `genero: consulta.genero ?? genero ?? 0,`

Prioridad: campo genero explícito de la nota médica > derivación de sexoCURP > 0.

**3. Actualizar valueByField — derechohabiencia (línea 348):**

ANTES: `derechohabiencia: '99',`
DESPUÉS: `derechohabiencia: consulta.derechohabiencia || '99',`

Usa `||` (no `??`) porque string vacío también debe caer al default '99'.

**4. Actualizar valueByField — peso (línea 364):**

ANTES: `peso: 999,`
DESPUÉS: `peso: consulta.peso ?? 999,`

**5. Actualizar valueByField — talla (línea 365):**

ANTES: `talla: 999,`
DESPUÉS: `talla: consulta.talla ?? 999,`

**6. Actualizar valueByField — circunferenciaCintura (línea 366):**

ANTES: `circunferenciaCintura: 0,`
DESPUÉS: `circunferenciaCintura: consulta.circunferenciaCintura ?? 0,`

**7. Actualizar valueByField — glucemia (línea 388):**

ANTES: `glucemia: 0,`
DESPUÉS: `glucemia: consulta.glucemia ?? 0,`

**8. Actualizar valueByField — tipoMedicion (línea 389):**

ANTES: `tipoMedicion: -1,`
DESPUÉS:
```typescript
tipoMedicion: (consulta.glucemia != null && consulta.glucemia !== 0)
  ? (consulta.tipoMedicion ?? -1)
  : -1,
```

Solo lee tipoMedicion si glucemia es real (≠0, ≠null). Si glucemia es 0 o null → siempre -1 (NA).

**9. Actualizar valueByField — resultadoObtenidoaTravesde (línea 390):**

ANTES: `resultadoObtenidoaTravesde: -1,`
DESPUÉS:
```typescript
resultadoObtenidoaTravesde: (consulta.glucemia != null && consulta.glucemia !== 0)
  ? (consulta.resultadoObtenidoaTravesde ?? -1)
  : -1,
```

Misma lógica condicional que tipoMedicion.
  </action>
  <verify>
1. `cd backend && npx tsc --noEmit` — compila sin errores
2. Verificar que no quedan valores hardcoded para los 8 campos:
   `grep -n "peso: 999\b" backend/src/modules/giis-export/transformers/cex.mapper.ts` — NO debe haber match (ahora es `consulta.peso ?? 999`)
   `grep -n "derechohabiencia: '99'" backend/src/modules/giis-export/transformers/cex.mapper.ts` — NO debe haber match solo
3. Tests existentes (si hay): `cd backend && npx jest --testPathPattern cex` — deben pasar (los tests usan consulta sin los 8 campos, los fallbacks mantienen comportamiento idéntico)
  </verify>
  <done>
ConsultaExternaLike tiene los 8 campos opcionales. valueByField lee los 8 campos de `consulta` con fallbacks correctos. Tests existentes pasan sin cambios (backward compatible por fallbacks).
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar PDF — interface y secciones nuevas</name>
  <files>backend/src/modules/informes/documents/nota-medica.informe.ts</files>
  <action>
**1. Extender interface NotaMedica (líneas 269-299):**

Agregar 11 campos opcionales después de `causaExterna?`:
```typescript
// CEX: Datos demográficos
genero?: number;
derechohabiencia?: string;
// CEX: Somatometría
peso?: number;
talla?: number;
circunferenciaCintura?: number;
indiceMasaCorporal?: number;
categoriaIMC?: string;
categoriaCircunferenciaCintura?: string;
// CEX: Glucemia
glucemia?: number;
tipoMedicion?: number;
resultadoObtenidoaTravesde?: number;
```

**2. Crear función construirDatosDemograficos (antes de notaMedicaInforme):**

Patrón similar a `construirSignosVitales`. Retorna Content | null.

```typescript
function construirDatosDemograficos(notaMedica): Content | null {
  const datos = [];

  // Género
  if (notaMedica.genero != null && notaMedica.genero !== 0) {
    const etiquetasGenero = {
      1: 'Masculino', 2: 'Femenino', 3: 'Transgénero',
      4: 'Transexual', 5: 'Travesti', 6: 'Intersexual', 88: 'Otro',
    };
    datos.push({ text: 'Género: ', bold: true });
    datos.push({ text: etiquetasGenero[notaMedica.genero] || 'No especificado' });
  }

  // Derechohabiencia
  if (notaMedica.derechohabiencia && notaMedica.derechohabiencia !== '0' && notaMedica.derechohabiencia !== '99') {
    const catalogoDerechohabiencia = {
      '1': 'Ninguna', '2': 'IMSS', '3': 'ISSSTE', '4': 'PEMEX',
      '5': 'SEDENA', '6': 'SEMAR', '8': 'Otra', '10': 'IMSS Bienestar',
      '11': 'ISSFAM', '14': 'OPD IMSS BIENESTAR',
    };
    const etiquetas = notaMedica.derechohabiencia.split('&')
      .map(v => catalogoDerechohabiencia[v] || v)
      .join(', ');
    if (datos.length > 0) datos.push({ text: '  |  ' });
    datos.push({ text: 'Derechohabiencia: ', bold: true });
    datos.push({ text: etiquetas });
  }

  if (datos.length === 0) return null;
  return {
    text: datos,
    margin: [0, 0, 0, 10],
    style: 'paragraph',
  };
}
```

**3. Crear función construirSomatometria (antes de notaMedicaInforme):**

```typescript
function construirSomatometria(notaMedica): Content | null {
  const datos = [];

  const agregarDato = (etiqueta, valor, unidad) => {
    if (valor != null && valor !== 0 && valor !== 999) {
      if (datos.length > 0) datos.push({ text: '  |  ' });
      datos.push({ text: ` ${etiqueta}: `, bold: true });
      datos.push({ text: `${valor}${unidad}` });
    }
  };

  agregarDato('Peso', notaMedica.peso, ' kg');
  agregarDato('Talla', notaMedica.talla, ' cm');

  // IMC (solo si fue calculado)
  if (notaMedica.indiceMasaCorporal != null && notaMedica.indiceMasaCorporal > 0) {
    if (datos.length > 0) datos.push({ text: '  |  ' });
    datos.push({ text: ' IMC: ', bold: true });
    datos.push({ text: `${notaMedica.indiceMasaCorporal.toFixed(1)}` });
    if (notaMedica.categoriaIMC) {
      datos.push({ text: ` (${notaMedica.categoriaIMC})` });
    }
  }

  // Circunferencia cintura (0 = desconoce, no mostrar)
  if (notaMedica.circunferenciaCintura != null && notaMedica.circunferenciaCintura > 0) {
    if (datos.length > 0) datos.push({ text: '  |  ' });
    datos.push({ text: ' C. Cintura: ', bold: true });
    datos.push({ text: `${notaMedica.circunferenciaCintura} cm` });
    if (notaMedica.categoriaCircunferenciaCintura) {
      datos.push({ text: ` (${notaMedica.categoriaCircunferenciaCintura})` });
    }
  }

  if (datos.length === 0) return null;
  return {
    text: [{ text: 'Somatometría: ', bold: true }, ...datos],
    margin: [0, 0, 0, 10],
    style: 'paragraph',
  };
}
```

**4. Crear función construirGlucemia (antes de notaMedicaInforme):**

```typescript
function construirGlucemia(notaMedica): Content | null {
  if (notaMedica.glucemia == null || notaMedica.glucemia === 0) return null;

  const datos = [];
  datos.push({ text: 'Glucemia: ', bold: true });
  datos.push({ text: `${notaMedica.glucemia} mg/dl` });

  if (notaMedica.tipoMedicion != null && notaMedica.tipoMedicion !== -1) {
    datos.push({ text: '  |  ' });
    datos.push({ text: 'Ayunas: ', bold: true });
    datos.push({ text: notaMedica.tipoMedicion === 1 ? 'Sí' : 'No' });
  }

  if (notaMedica.resultadoObtenidoaTravesde != null && notaMedica.resultadoObtenidoaTravesde !== -1) {
    datos.push({ text: '  |  ' });
    datos.push({ text: 'Medido por: ', bold: true });
    datos.push({ text: notaMedica.resultadoObtenidoaTravesde === 1 ? 'Laboratorio' : 'Tira de glucosa capilar' });
  }

  return {
    text: datos,
    margin: [0, 0, 0, 10],
    style: 'paragraph',
  };
}
```

**5. Insertar las 3 funciones en el content[] del documento (después de construirSignosVitales, línea ~557):**

Después de:
```typescript
construirSignosVitales(notaMedica),
```

Agregar:
```typescript
construirSomatometria(notaMedica),
construirGlucemia(notaMedica),
construirDatosDemograficos(notaMedica),
```

Las funciones retornan `null` cuando no hay datos, y pdfmake ignora `null` en arrays de content.

NOTA: `construirDatosDemograficos` se inserta al final de las nuevas secciones (o puede ir antes de signos vitales para seguir el orden clínico). Elegir ubicación lógica: datos demográficos antes de signos vitales, somatometría y glucemia después.

Orden recomendado en content[]:
1. construirDatosDemograficos(notaMedica) — antes o después de exploración física
2. construirSignosVitales(notaMedica) — existente
3. construirSomatometria(notaMedica) — nuevo
4. construirGlucemia(notaMedica) — nuevo
  </action>
  <verify>
1. `cd backend && npx tsc --noEmit` — compila sin errores
2. Verificar que las 3 funciones existen:
   `grep -c "function construir" backend/src/modules/informes/documents/nota-medica.informe.ts`
   Debe ser ≥4 (construirSignosVitales + 3 nuevas)
3. Verificar que la interface NotaMedica tiene los 11 campos:
   `grep -c "genero\|derechohabiencia\|peso\|talla\|circunferenciaCintura\|glucemia\|tipoMedicion\|resultadoObtenidoaTravesde\|indiceMasaCorporal\|categoriaIMC\|categoriaCircunferenciaCintura" backend/src/modules/informes/documents/nota-medica.informe.ts`
  </verify>
  <done>
Interface NotaMedica tiene 11 nuevos campos. 3 funciones construir* creadas. content[] del PDF incluye las 3 secciones opcionales. Notas existentes sin datos nuevos generan el mismo PDF (funciones retornan null).
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — sin errores de compilación
2. Tests CEX existentes pasan (fallbacks preservan comportamiento original)
3. Interface ConsultaExternaLike tiene 8 campos opcionales
4. valueByField en cex.mapper.ts lee de `consulta` con fallbacks para los 8 campos
5. Interface NotaMedica en nota-medica.informe.ts tiene 11 campos opcionales
6. 3 funciones construir* existen y se invocan en content[]
7. PDF de nota sin datos nuevos es idéntico al anterior (funciones retornan null)
</verification>

<success_criteria>
- Mapper CEX alimentado con datos reales de la nota médica para los 8 campos (solo se ejecuta para SIRES, sin riesgo para SIN_REGIMEN)
- Fallbacks mantienen backward compatibility (documentos sin los campos → mismos defaults que antes)
- PDF muestra somatometría cuando peso/talla/cintura tienen datos (providers SIRES que capturaron los datos)
- PDF muestra glucemia cuando glucemia>0
- PDF muestra género/derechohabiencia cuando están especificados
- Documentos existentes y documentos de providers SIN_REGIMEN generan PDF idéntico al anterior (las funciones construir* retornan null cuando no hay datos)
- Los reportes GIIS CEX exportan los valores reales capturados por el médico en lugar de los defaults hardcoded
</success_criteria>

<output>
After completion, create `backend/.planning/phases/07-campos-cex-nota-medica/07-03-SUMMARY.md`
</output>
